/*$.each(nodeList, function () {
               mapCtx.beginPath();
               mapCtx.strokeStyle = 'red';
               mapCtx.strokeWidth = 1;
               mapCtx.moveTo($this.x1 * mapRateX, $this.y1 * mapRateY);
               mapCtx.lineTo($this.x2 * mapRateX, $this.y2 * mapRateY);
               mapCtx.stroke();
               mapCtx.closePath();
           });*/

// 전체 노드 시작 끝점 출력
/*$.each(nodeXYList, function () {
    mapCtx.beginPath();
    mapCtx.fillStyle = 'blue';
    mapCtx.moveTo($this.x * mapRateX, $this.y * mapRateY);
    mapCtx.arc($this.x * mapRateX, $this.y * mapRateY, 10, 0, 2 * Math.PI);
    mapCtx.fill();
    mapCtx.closePath();
});*/

/*mapCtx.beginPath();
           mapCtx.fillStyle = 'blue';
           mapCtx.moveTo(0, 0);
           mapCtx.arc(startXY.x, startXY.y, 10, 0, 2 * Math.PI);
           mapCtx.fill();
           mapCtx.closePath();*/
